<?php
// api/auth.php?id=auth
if (isset($_REQUEST["id"]) == "auth") {
    // registration | sign up | log in

    // make data connection, send to function to validate and add the return
    // $_REQUEST["getDesc"]

}

// echo "Aye !";
